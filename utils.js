// // backend/utils.js
// function extractImageUrlFromResponse(response) {
//   const match = response.match(/https?:\/\/[^\s]+/);
//   return match ? match[0] : null;
// }

// module.exports = { extractImageUrlFromResponse };
